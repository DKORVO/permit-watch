# Ottawa and MERX pages

Upload these three files to their existing matching GitHub folders:

* `app/views.py`
* `app/templates/index.html`
* `app/static/app.css`

The existing homepage (`/`) becomes the City of Ottawa page. A new **MERX**
tab opens `/merx`, which is a placeholder until a MERX source is configured.
